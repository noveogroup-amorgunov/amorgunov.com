<div class="posts">
  {% for post in postslist | reverse %}
    <div style="{% if post.data.bg %}background-image: url('{{ post.data.bg }}');{% endif %}" class="posts__item{% if post.url == url %} posts__item_active{% endif %}{% if post.data.bg %} posts__item_theme_light{% endif %}">
      <time class="posts__date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
      <a href="{{ post.url | url }}" class="posts__link {{post.data.class}}">{{ post.data.title }}</a>

      {% set tagUrl %}/tags/{{ post.data.tags[1] }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="posts__tag">
        {{ post.data.tags[1] }}
      </a>
    </div>
  {% endfor %}
</div>
