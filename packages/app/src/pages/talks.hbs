---
layout: layouts/next.hbs
title: Выступления
description: Выступления на конференциях и митапах
permalink: /talks/
---

{{#> components/text type="h1" as="h1"}}Выступления{{/ components/text}}

<nav aria-label="Список выступлений" class="talks-list-container">
  {{#each talks as |talkInYear|}}
    <div>
      {{#> components/text type="h1" as="h2"}}{{@key}}{{/ components/text}}

      <ul class="talks-list">
        {{#each talkInYear as |talk|}}
            <li class="talks-list__item" style="background-image: url({{talk.imageUrl}}); {{#if talk.wide}}width: calc(100% - 1rem);{{/if}}">
                <div class="talks-list__item-title">
                  <a class="talks-list__item-link" href="{{talk.conferenceUrl}}" target="_blank" rel="noopener noreferrer">
                    <strong>{{talk.conference}}</strong> / <strong>{{talk.title}}</strong>
                  </a>
                </div>
                <div class="talks-list__item-links">
                  {{#if talk.videoUrl}}<a class="talks-list__item-link-additional" href="{{talk.videoUrl}}">🍿 Video</a>{{/if}}
                  {{#if talk.slidesUrl}}<a class="talks-list__item-link-additional" href="{{talk.slidesUrl}}">📸 Slides</a>{{/if}}
                  {{#if talk.materialsUrl}}<a class="talks-list__item-link-additional" href="{{talk.materialsUrl}}">📒 Materials</a>{{/if}}
                </div>
            </li>
          </a>
        {{/each}}
      </ul>
    </div>
  {{/each}}
</nav>
