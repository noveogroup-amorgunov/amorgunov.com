{
  "name": "amorgunov-com",
  "private": true,
  "scripts": {
    "start": "pnpm --filter app start",
    "build": "pnpm --filter app build",
    "lint": "pnpm --filter app lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
